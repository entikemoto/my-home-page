---
title: "Claude Code 更新チェック（定期チェック実施）"
date: "2026-07-02"
summary: "- 変わったこと: v2.1.190〜v2.1.198（8バージョン）を確認。Claude Sonnet 5 がデフォルトモデルに（v2.1.197、100万トークンネイティブコンテキスト、$2/$10 per Mtok、8/31まで promotional）。`/dataviz` skill 追加・background agent の自動コミット→Draft PR作成・通知フック追加（v2.1.198）。"
tags:
  - "claude-code"
  - "workflow"
  - "changelog-check"
  - "dev-env-log"
vault_hp_sync: true
vault_sync_source: ccp
---

### この回の要点（先に読む）

- **変わったこと**: v2.1.190〜v2.1.198（8バージョン）を確認。**Claude Sonnet 5 がデフォルトモデルに**（v2.1.197、100万トークンネイティブコンテキスト、$2/$10 per Mtok、8/31まで promotional）。`/dataviz` skill 追加・background agent の自動コミット→Draft PR作成・通知フック追加（v2.1.198）。
- **このVaultへの影響**: **モデル更新あり（マイナー以上）→ `/methods-audit` 前倒しトリガー該当**。CLAUDE.md の「デフォルト Sonnet 4.6」記述が陳腐化。
- ⚠️ **検証について**: Gemini CLI の一次調査結果に GitHub CHANGELOG.md で確認できない記述（npm単体インストール廃止、Fable 5 の7/7提供終了日）が含まれていたため、`gh api repos/anthropics/claude-code/contents/CHANGELOG.md` で直接裏取りし、該当項目は除外・バージョン番号を訂正した。

- 実行時バージョン: **v2.1.198**
- 調査範囲: v2.1.191〜v2.1.198（GitHub CHANGELOG.md 直接取得で検証）
- モデル更新: **あり（Claude Sonnet 5 がデフォルトに）** → `/methods-audit` 前倒し実行

#### 発見した新機能と対応

- **Claude Sonnet 5 がデフォルトモデルに**（v2.1.197）: 即適用 → `/methods-audit` で `methods/` 全体のモデル参照を更新
- **`/dataviz` skill**（v2.1.198）: 確認のみ — 既にVaultで利用可能と確認済み
- **background agent の自動コミット→Draft PR作成**（v2.1.198）: 検討 — worktree内作業完了時に自動でPRが作られる。Vaultの `projects/` 開発フローに合わせた運用ルール検討が必要
- **`agent_needs_input`/`agent_completed` 通知フック**（v2.1.198）: 検討 — Push notification 有効化（繰越タスク）と合わせて設定候補
- **`/rewind`**（v2.1.191、正: Geminiは2.1.190と誤記）: 確認のみ — `/clear` 前の会話状態に復元可能
- **Extended Thinking 設定の継承**（v2.1.198）: 自動適用 — サブエージェントが親セッションの推論深度を継承
- **Explore agent のモデル継承**（v2.1.198、haiku固定→opus上限で親を継承）: 自動適用
- **`CLAUDE_CODE_DISABLE_MOUSE_CLICKS`**（v2.1.195、正: Geminiは2.1.197と誤記）: 不要 — 現状の運用に影響なし
- **`sandbox.credentials`**（v2.1.187、前回チェックから持ち越し）: 検討継続 — PHIプロジェクト（VoiceClinical）への適用候補

#### 除外した記述（Gemini CLI 調査の未検証・要修正項目）

- ~~「npm単体インストール廃止」~~ — GitHub CHANGELOG.md に記載なし。捏造の疑い
- ~~「Fable 5 プロモ提供は2026-07-07まで」~~ — 裏付けなし。捏造の疑い
- ~~「Remote MCP SSE/HTTP Streaming が新機能」~~ — 該当バージョン範囲の CHANGELOG に記載なし

#### 適用した変更

- `docs/last_changelog_check.md` の最終チェック日を `2026-07-02` に更新
- `/methods-audit` を前倒し実行（本セッション内で継続）

#### 繰越タスク

- **Dynamic Workflows を試す**（前回繰越）
- **Push notification の有効化** + `CLAUDE_CLIENT_PRESENCE_FILE` 設定（前回繰越）
- **Hook → MCP tool 直接呼び出し設計**（前回繰越）
- **background agent 自動PR作成の運用ルール検討**（新規）

---
