---
title: "Claude Code 更新チェック（定期チェック実施）"
date: "2026-05-06"
summary: "- 変わったこと: 新リリースなし（v2.1.128 据え置き）。繰越タスク3件を処理。"
tags:
  - "claude-code"
  - "workflow"
  - "changelog-check"
  - "dev-env-log"
vault_hp_sync: true
vault_sync_source: ccp
---

### この回の要点（先に読む）

- **変わったこと**: 新リリースなし（v2.1.128 据え置き）。繰越タスク3件を処理。
- **いままでとの違い**: archive ワークフローが手動2ステップだったが、`claude project purge` が自動化された。
- **メリット**: プロジェクト凍結時に Claude Code の内部状態（トランスクリプト・タスク履歴）も一括削除できる。

- 実行時バージョン: **v2.1.128**（変化なし）
- 新リリース: なし

#### 対応した繰越タスク

- **`/ultrareview` を試す** ✅ 完了（前セッション）— CortexFlow で3件のバグ発見・修正済み
- **Semgrep MCP** ✅ 廃止決定 — クラウド送信問題のため使用しない（代替: `/security-review`・ruff）
- **`claude project purge` の archive ワークフロー組み込み** ✅ 完了
  - `_tools/archive_project.py` に `purge_claude_state()` を追加
  - 移動前のパスで `claude project purge` を自動実行
  - `--skip-purge` オプションでスキップ可能
  - lint エラー（既存行の E501）も同時修正

#### 次回の最優先候補

- **Push notification の有効化** — claude.ai Web設定・機能ページ両方を確認済みだが設定項目なし。段階的ロールアウト未開放と判断。モバイルアプリ（claude.ai iOS/Android）でも未確認のため、次回モバイルアプリで確認する。
- **Hook → MCP tool 直接呼び出し設計** — Semgrep 廃止により再検討が必要（他 MCP への応用を検討）

---
