---
title: "Claude Code 更新チェック（定期チェック実施）"
date: "2026-06-10"
summary: "- 変わったこと: Claude Fable 5（Mythos-class）が 2026-06-09 リリース。Claude Code v2.1.170 で対応。v2.1.169 で `--safe-mode`・`/cd` コマンドも追加。"
tags:
  - "claude-code"
  - "workflow"
  - "changelog-check"
  - "dev-env-log"
vault_hp_sync: true
vault_sync_source: ccp
---

### この回の要点（先に読む）

- **変わったこと**: **Claude Fable 5**（Mythos-class）が 2026-06-09 リリース。Claude Code v2.1.170 で対応。v2.1.169 で `--safe-mode`・`/cd` コマンドも追加。
- **このVaultへの影響**: Fable 5 は API 課金モデル（$10/$50 per M tokens）。`claude-fable` コマンドセットアップ完了。CLAUDE.md にエスカレーション条件を追加。

- 実行時バージョン: **v2.1.170**（v2.1.168 → 更新済み）
- 調査範囲: v2.1.169〜v2.1.170（GitHub Releases 直接取得）
- モデル更新: **Claude Fable 5 リリース（2026-06-09）** → `/methods-audit` 前倒しトリガー該当

#### 発見した新機能と対応

- **Claude Fable 5**（v2.1.170, 2026-06-09）: 🔴 対応完了 — model ID: `claude-fable-5`、alias: `fable`。`~/.zshrc` に `claude-fable()` 関数追加。`~/.claude/CLAUDE.md` にエスカレーション条件追加
- **`--safe-mode` / `CLAUDE_CODE_SAFE_MODE`**（v2.1.169）: 検討 — CLAUDE.md・hooks・MCP・スキルを全無効化してトラブルシューティング起動。hooks 誤動作時の診断用
- **`/cd` コマンド**（v2.1.169）: 検討 — プロンプトキャッシュを壊さずに作業ディレクトリを変更
- **`disableBundledSkills` 設定**（v2.1.169）: 不要 — カスタムスキル中心の運用に影響しない

#### 適用した変更

- Claude Code を **v2.1.168 → v2.1.170** に更新（`claude update`）
- `~/.zshrc` に `claude-fable()` 関数を追加（`ANTHROPIC_API_KEY` は `~/.zprofile` から自動取得）
- `~/.claude/CLAUDE.md` にモデル選択ラダー・Fable 5 推薦条件・エスカレーション表を更新
- `docs/last_changelog_check.md` の最終チェック日を `2026-06-10` に更新

#### 繰越タスク

- **`/methods-audit` 前倒し実行** — Fable 5 が最上位モデルになったため `methods/` 全体のモデル参照を更新要
- **Dynamic Workflows を試す** — `ultracode` で起動可能
- **Push notification の有効化**（手動設定が必要）
- **Hook → MCP tool 直接呼び出し設計**

---
